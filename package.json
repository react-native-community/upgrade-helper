{
  "name": "web",
  "version": "0.1.0",
  "homepage": "https://pvinis.github.io/purge-web",
  "license": "MIT",
  "dependencies": {
    "ramda": "^0.26.1",
    "react": "^16.8.0",
    "react-dom": "^16.8.0",
    "react-ga": "^2.5.7",
    "react-github-btn": "^1.0.0",
    "react-native-web": "^0.11.0",
    "react-scripts": "3.0.1",
    "semver": "^6.0.0"
  },
  "devDependencies": {
    "eslint": "^5.16.0",
    "eslint-plugin-prettier": "^3.1.0",
    "gh-pages": "^2.0.0",
    "prettier": "^1.17.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "lint": "eslint . --cache --report-unused-disable-directives",
    "eject": "react-scripts eject",
    "predeploy": "yarn build",
    "deploy": "gh-pages --repo -d build",
    "deploy-ci": "gh-pages --repo https://$GH_TOKEN@github.com/pvinis/purge-web.git -d build"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
